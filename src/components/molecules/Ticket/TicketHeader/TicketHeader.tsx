import React from 'react'
import TicketHeaderStyled from './TicketHeaderStyled'

interface IProps {}

const TicketHeader = (props: IProps) => {
  return <TicketHeaderStyled />
}

export default TicketHeader
