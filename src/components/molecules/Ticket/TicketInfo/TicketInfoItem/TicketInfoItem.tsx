import React from 'react'
import TicketInfoItemStyled from './TicketInfoItemStyled'

interface IProps {}

const TicketInfoItem = (props: IProps) => {
  return <TicketInfoItemStyled />
}

export default TicketInfoItem
